export default function SidebarBrand({ fontSize = '1.4rem' }) {
    return (
        <h4 className="fw-bold mb-3" style={{ fontSize }}>
            Trace<span style={{ color: '#28a745' }}>≡</span>Mat
        </h4>
    );
}
